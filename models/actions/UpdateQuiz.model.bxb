action (UpdateQuiz) {
  type (Search)
  description (__DESCRIPTION__)
  collect {
    input (answer) {
      type (Answer)
      min (Required) max (One)
    }
    input (quiz) {
      type (Quiz)
      min (Required) max (One)
    }
  }
  output (Quiz)
}
